# Walk

Walk is a statically-typed, compiled scripting language designed for automation, tooling, and system-level scripting. Its core philosophy is: the file system is the architecture. Walk entirely eliminates the concept of mega-files, instead enforcing modularity.
