# Interfaces

- Interfaces are declared in `.req` files. A structure must explicitly declare the interfaces they fulfill using the `req` keyword at the top of the `.str` file.
- Interfaces require fulfilment of certain methods. This requires absolute signature equality (including strict matching on the `!` error operator).
