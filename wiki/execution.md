# Execution

Every public `.fun` file can be ran directly from the command line.

```
walk path/to/function.fun arg1 arg2 ...
```

It is recommended that the root of a project contains a ``main.fun`` file to serve as entry-point for the project and to have access permission to all subdirectories.
