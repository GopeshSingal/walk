# Privacy

Any file or directory prefixed with an underscore (e.g., `_format.fun` or `_helpers/`) is strictly private. It may only be accessed by sibling files within the exact same parent directory.
