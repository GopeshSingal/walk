# Explicit Composition

Object-oriented inheritance is not supported. Walk uses structure embedding (composition). Implicit delegation is not implemented yet. Developers must explicitly write out the full path for embedded method calls.
